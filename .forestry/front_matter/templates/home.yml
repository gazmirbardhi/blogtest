---
label: Home
hide_body: true
fields:
- type: text
  name: title
  label: Title
  description: The title of the page.
  config:
    required: true
- name: illustration_image
  type: file
  config:
    maxSize: 250
  label: illustration image
- name: build_image
  type: file
  config:
    maxSize: 250
  label: Build Image
- name: blog_image
  type: file
  config:
    maxSize: 250
  label: Blog Image
- name: everything_else_image
  type: file
  config:
    maxSize: 250
  label: Everything else Image
- type: blocks
  name: sections
  label: Sections
  description: Home page sections
  template_types:
  - intro
- type: text
  name: template
  label: Template
  hidden: true
  default: home
  config:
    required: true
- name: quora_icon
  type: file
  config:
    maxSize: 250
  label: quora icon
- name: medium_icon
  type: file
  config:
    maxSize: 250
  label: medium icon
- name: twitter_icon
  type: file
  config:
    maxSize: 250
  label: twitter icon
pages:
- src/pages/index.md
